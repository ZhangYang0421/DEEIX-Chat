{
  "name": "@deeix/chat-monorepo",
  "version": "0.4.1",
  "private": true,
  "packageManager": "pnpm@10.17.0",
  "scripts": {
    "api:check": "pnpm --filter @deeix/api-contract check",
    "api:generate": "pnpm --filter @deeix/api-contract generate",
    "build": "turbo run build",
    "check": "turbo run check",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "dev:api": "turbo run dev --filter=@deeix/api",
    "dev:web": "turbo run dev --filter=@deeix/web",
    "test": "turbo run test",
    "upstream:impact": "node scripts/upstream-impact.mjs",
    "upstream:check": "node scripts/upstream-impact.mjs --no-fetch --run-checks",
    "upstream:baseline": "node scripts/upstream-impact.mjs --no-fetch --set-baseline",
    "verify": "turbo run check test build"
  },
  "devDependencies": {
    "turbo": "2.10.5"
  },
  "pnpm": {
    "overrides": {
      "dompurify": "3.4.14"
    }
  }
}
